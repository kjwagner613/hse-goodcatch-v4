<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <link rel="icon" href="/favicon.ico" />
    <title>Edit GoodCatch</title>
  </head>
  <body>
    <%- include("../partials/_navbar.ejs") %>
    <h1 class="h1-edit4"></h1> >Edit GoodCatch Record</h1>
    <div class="data-grid-edit4">
      <form action="/goodCatch/<%= goodCatch._id %>?_method=PUT" method="POST">
        <!-- Hidden input for CSRF token -->
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />

        <div class="grid-item4">
          <label for="site">Site:</label>
          <input type="text" name="site" id="site" value="<%= goodCatch.site %>" required />
        </div>

        <div class="grid-item4">
          <label for="department">Department:</label>
          <input type="text" name="department" id="department" value="<%= goodCatch.department %>" required />
        </div>

        <div class="grid-item4">
          <label for="area">Area:</label>
          <input type="text" name="area" id="area" value="<%= goodCatch.area %>" required />
        </div>

        <div class="grid-item4">
          <label for="category">Event Category:</label>
          <input type="text" name="category" id="category" value="<%= goodCatch.events[0]?.category || '' %>" required />
        </div>

        <div class="grid-item4">
          <label for="description">Event Description:</label>
          <textarea name="description" id="description" rows="4" required><%= goodCatch.events[0]?.description || '' %></textarea>
        </div>

        <div class="grid-item4">
          <button type="submit" class="submit-btn">Save Changes</button>
        </div>
      </form>
    </div>
  </body>
</html>